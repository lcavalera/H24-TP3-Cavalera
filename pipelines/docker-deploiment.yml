# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- none

variables:
  serviceConnection: 'sc-azure'
  appName: 'webapp-emplois'
  containerRegistry: 'acrmodernrecruit2.azurecr.io'
  acrServiceConnection: 'sc-container-registry2'
  imageRepository: 'emploisapi'
  aciName: 'aci-emplois-api'
  dockerfilePath: '**/Dockerfile'
  tag: '$(Build.BuildId)'

stages:
  - stage: 'Build'
    displayName: 'Génération et push image'
    pool:
      vmImage: ubuntu-latest
    jobs:
      - job: 'Build'
        steps:
        - task: Docker@2
          displayName: 'Génération et push image dans registre de conteneurs'
          inputs:
            containerRegistry: '$(acrServiceConnection)'
            repository: '$(imageRepository)'
            command: 'buildAndPush'
            Dockerfile: '$(dockerfilePath)'
            buildContext: '$(Build.Repository.LocalPath)'

  - stage: 'DeployToDev'
    displayName: 'Déploiment en Dev'
    pool:
      vmImage: ubuntu-latest
    dependsOn: 'Build'
    jobs:
      - deployment: 'DeployToDev'
        environment: Dev
        strategy:
          runOnce:
            deploy:
              steps:
              - task: AzureCLI@2
                displayName: 'Déploiment sur Azure Container Instances'
                inputs:
                  azureSubscription: $(serviceConnection)
                  scriptType: 'bash'
                  scriptLocation: 'inlineScript'
                  inlineScript: 
                    az container create --resource-group rg-dev-modernrecruit --name $(aciName) --environment-variables ASPNETCORE_ENVIRONMENT=development --image $(containerRegistry)/$(imageRepository):$(tag) --cpu 1 --memory 1 --registry-login-server $(containerRegistry) --registry-username acrmodernrecruit2 --registry-password Fs3pE8/YJdOCjy6XpHh1grr2fjLyb9E5ikwYvBdjsS+ACRAnIIjo --ip-address Public --dns-name-label emploisapi --ports 80